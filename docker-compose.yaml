version: '3.8'

services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    networks:
      - proxy
  nginx:
    image: nginx:latest
    environment:
      - FLASK_APP_DOMAIN=${FLASK_APP_DOMAIN}
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    depends_on:
      - flask-app
    networks:
      - proxy

networks:
  proxy:
    name: proxy
    driver: bridge
